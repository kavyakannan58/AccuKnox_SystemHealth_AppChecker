# AccuKnox_SystemHealth_AppChecker
Here’s a concise version within **350 characters**:  > Python project with two automation scripts: one monitors CPU, memory, and disk usage, while the other checks application uptime using HTTP status codes. Provides real-time system insights, logs performance data, and alerts when thresholds are exceeded or services are unavailable.
